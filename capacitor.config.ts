import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yaabin.app',
  appName: 'Yaabin',
  webDir: 'dist/',
  bundledWebRuntime: false
};

export default config;
